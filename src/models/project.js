class Project {
    constructor(id, title, isHtml, isCss, isJs, isReact, isReactNative, isNode) {
        this.id = id
        this.title = title
        this.isHtml = isHtml
        this.isCss = isCss
        this.isJs = isJs
        this.isReact = isReact
        this.isReactNative = isReactNative
        this.isNode = isNode
    }
}

export default Project